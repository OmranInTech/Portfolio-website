[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  # Use a stable Node.js version supported by Netlify build image
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
